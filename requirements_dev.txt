# Development requirements for npyx
# Install with: pip install -e .[dev] or uv pip install -e .[dev]

# Build and packaging tools
build>=1.0.0
twine>=4.0.0
pip>=23.0

# Modern Python environments
uv>=0.1.0
